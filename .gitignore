# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Ignore any .tfvars files that are generated automatically for each Terraform run.
# Most .tfvars files are managed as part of configuration and so should be included in
# version control.
*.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files
*.tfplan
*.tfplan.binary

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Ignore the .terraform.lock.hcl file if you are not using a specific provider version lock.
# However, it is generally recommended to commit this file to ensure consistent provider versions
# across environments. Uncomment the following line if you choose not to commit it.
# .terraform.lock.hcl

# Ignore any editor-specific files or directories
.vscode/
.idea/
.DS_Store
*.swp
*.bak
*~